import React from 'react';

const LoginPage = (props) => {
    console.log(props);
    return (
        <>
            <main>
                LOGIN PAGE
            </main>
        </>
    );
};

export default LoginPage;